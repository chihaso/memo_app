<!DOCTYPE html>
<html lang="ja" dir="ltr">

  <head>
    <meta charset="utf-8">
    <title>ちはそのメモアプリ_編集</title>
    <link rel="stylesheet" type="text/css" href="./css/general.css">
  </head>

  <body>
    <article>
      <h1>MEMO</h1>
      <h2>メモ<%=@num%>:</h2>
      <form method=post action=/update_<%=@num%> class="create">
        <input type="hidden",  name="_method", value="PATCH">
        <% memo_text = File.open("./memo_data/#{@num}").read %>
        <textarea name="memo"><%=memo_text%></textarea>
        <input type="submit" value="保存" class="button save">
      </form>
    </article>
  </body>
