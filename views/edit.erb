<!DOCTYPE html>
<html lang="ja" dir="ltr">

  <head>
    <meta charset="utf-8">
    <title>ちはそのメモアプリ_編集</title>
  </head>

  <body>
    <article>
      <h1>MEMO</h1>
      <form method=post action=/update_<%=@num%>>
        <input type="hidden",  name="_method", value="PATCH">
        <% memo_text = File.open("./memo_data/#{@num}").read %>
        <textarea name="memo"><%=memo_text%></textarea>
        <input type="submit" value="保存">
      </form>
      </ul>
    </article>
  </body>
