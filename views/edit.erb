<!DOCTYPE html>
<html lang="ja" dir="ltr">

  <head>
    <meta charset="utf-8">
    <title>ちはそのメモアプリ_メモ編集</title>
    <link rel="stylesheet" type="text/css" href="../../css/general.css">
  </head>

  <body>
    <article>
      <div class="top_containor">
        <h1><a href="/">MEMO</a></h1>
      </div>
      <h2>メモ:</h2>
      <form method=post action=/<%=@id%> class="create">
        <input type="hidden", name="_method", value="PATCH">
        <% memo_text = File.open("./memo_data/#{@id}").read %>
        <textarea name="memo"><%=memo_text%></textarea>
        <input type="submit" value="保存" class="button save">
      </form>
    </article>
  </body>
